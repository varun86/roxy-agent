services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: my-deer-flow-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333" # HTTP API + Web UI
      - "6334:6334" # gRPC API
    volumes:
      - ./data/qdrant:/qdrant/storage
